{
  "name": "julina-candles-melts",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client install --production=false && npm --prefix client run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "cloudinary": "^2.10.0",
    "razorpay": "^2.9.8",
    "vite": "^8.2.0"
  }
}
